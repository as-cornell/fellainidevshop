{# 
/**
 * Ouptuts the field content for the componity bundle "sat block"
 * Box is just a box to put stuff in.
 * output a stat block. sets the modifier to the array length to set flex on enclosed items
 */
 #}


{% set length = items|length %}
{% set group_base_class = 'statList' %}
{% set group_modifiers = [length] %}


{% include "@organisms/stat-list/_stat-list.twig" with {

} %}


